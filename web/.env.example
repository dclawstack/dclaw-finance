# URL of the deployed FastAPI backend.
# On Vercel: set this in Project Settings → Environment Variables.
# Locally: point at your running Docker backend.
BACKEND_URL=http://localhost:8096

# Exposed to the browser — only needed if you bypass the rewrite proxy.
# Leave blank when using next.config.js rewrites (recommended).
NEXT_PUBLIC_API_URL=
